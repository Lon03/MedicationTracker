# macOS
.DS_Store

# Xcode — per-user state, never shared
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout

# Build products
build/
DerivedData/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/

# Injected secrets / signing (none today; keeps them out if added later)
**/secrets.xcconfig

# Working material — not part of the deliverable (kept locally)
CLAUDE.md
docs/
.claude/
